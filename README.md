API, json server, axios kullanarak task projesi yapıldı.

npm create-react-app
npm install -g json-server
npm install axios
json-server --watch db.json --port 3004

kodları terminale girildikten sonra proje çalıştırılabilir.
